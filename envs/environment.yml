name: examples-gallery-manage
channels:
  - ae5-admin
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - doit
  - python-dotenv
  - pyyaml
  - anaconda-project
  - intake
  - nbclient
  - nbformat
  - ae5-tools
  - nbqa
  - flake8
  - pip:
    - nbval >= 0.10.0
  # - pydata-sphinx-theme <= 0.9.0
  # - sphinx-copybutton
  # # Somehow needed for nbsite's gallery
  # - holoviews
  # - pip:
  #   - git+https://git@github.com/pyviz-dev/nbsite.git@fix_inline_gallery
